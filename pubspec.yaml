name: diapce_aplicationn
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.4
  intl: ^0.20.2
  fl_chart: ^0.68.0             # Gráficas (pastel y línea)
  google_fonts: ^8.2.1
  http: ^1.2.2                  # Cliente REST hacia el backend Flask
  shared_preferences: ^2.3.3     # Token JWT (flutter_secure_storage requiere ATL en Windows)
  printing: ^5.15.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
